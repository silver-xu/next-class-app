import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
    token: {
        fontSize: 16,
        colorPrimary: "#83b540",
        colorText: "#707070",
    },
};

export default theme;
